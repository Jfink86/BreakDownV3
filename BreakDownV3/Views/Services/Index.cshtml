@model IEnumerable<BreakDownV3.Service>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Brake.Notes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Oil.Notes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TuneUp.Notes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vehicle.ID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Brake.Notes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Oil.Notes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TuneUp.Notes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Vehicle.ID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ServiceID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ServiceID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ServiceID })
        </td>
    </tr>
}

</table>
